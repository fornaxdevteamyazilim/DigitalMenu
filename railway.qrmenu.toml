# Yalnızca QR Menü Railway servisi için (Settings → Config file: railway.qrmenu.toml)
[build]
builder = "DOCKERFILE"
dockerfilePath = "src/DigitalMenu.QrMenu/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
