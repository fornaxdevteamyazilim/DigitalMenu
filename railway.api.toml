# Yalnızca API Railway servisi için (Settings → Config file: railway.api.toml)
[build]
builder = "DOCKERFILE"
dockerfilePath = "src/DigitalMenu.API/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
