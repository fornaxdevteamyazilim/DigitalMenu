# API Railway servisi — Config file: railway.api.toml
# Public URL: https://digitalmenu-production-72f0.up.railway.app
[build]
builder = "DOCKERFILE"
dockerfilePath = "src/DigitalMenu.API/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
